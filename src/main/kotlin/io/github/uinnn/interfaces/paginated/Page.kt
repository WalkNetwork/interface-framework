package com.side.lib.graphical.pagination

/*
class Page(val pag: Pagination) : View(pag.title, pag.rows) {
  var previousPage = copyButton(pag.previousPage.slot, pag.previousPage) {
    click { pag.openPrevious(player) }
    render {
      if (!pag.hasPrevious)
        item { type = Material.AIR }
      else
        item(inject(Pagination, pag))
    }
  }
  
  var nextPage = copyButton(pag.nextPage.slot, pag.nextPage) {
    click { pag.openNext(player) }
    render {
      if (!pag.hasNext)
        item { type = Material.AIR }
      else
        item(inject(Pagination, pag))
    }
  }
}

 */