package walkmc.graphical.schema

/**
 * The default schematic implementation when mapping
 * a scrollable graphical interface.
 */
class StandardSchema : Schema by 10 schema 34 excludes DefaultExcluder
